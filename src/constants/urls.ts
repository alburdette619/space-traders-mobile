export const getFactionImageUrl = (factionSymbol: string): string => {
  return `https://my.spacetraders.io/factions/${factionSymbol}/emblem.png`;
};

export const spaceTradersLogin = 'https://my.spacetraders.io/login';
